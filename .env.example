# Appfigures Personal Access Token (Bearer).
# Create an API Client at https://appfigures.com/developers/keys, pick scopes,
# then "Create Personal Access Token". The token is shown only once.
APPFIGURES_PAT=pat_yourtokenhere
